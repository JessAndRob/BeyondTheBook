on:
  workflow_dispatch:

name: Run Command
jobs:
  run_command:
    runs-on: ubuntu-latest
    steps:
    # Log into Azure
    - uses: azure/login@v2
      with:
        creds: ${{ secrets.AZURE_CREDENTIALS }}
    - name: Testing run-command
      shell: pwsh
      run: |
        az vm run-command invoke -g rg-pass-prod-001 -n jumpy --command-id RunPowerShellScript --scripts "$env:COMPUTERNAME; Get-DbaDatabase -SqlInstance sql1"




